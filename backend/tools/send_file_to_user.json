{
  "type": "function",
  "function": {
    "name": "send_file_to_user",
    "description": "将本地文件主动发送给用户。发送前会检查对应平台是否已绑定；未绑定则返回失败，请停止重试并告知用户前往设置中绑定。是否发送由你自行判断，系统不会自动推送。发送文本请使用 send_message_to_user。微信平台暂不支持发送文件。",
    "parameters": {
      "type": "object",
      "properties": {
        "file_path": {
          "type": "string",
          "description": "要发送的文件路径。支持相对路径（工作目录）或绝对路径。"
        },
        "platform": {
          "type": "string",
          "enum": ["微信", "QQ", "飞书"],
          "description": "目标推送平台。"
        }
      },
      "required": ["file_path", "platform"],
      "additionalProperties": false
    }
  }
}
