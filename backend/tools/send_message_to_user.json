{
  "type": "function",
  "function": {
    "name": "send_message_to_user",
    "description": "向用户主动发送文本消息。发送前会检查对应平台是否已绑定；未绑定则返回失败，请停止重试并告知用户前往设置中绑定。是否发送由你自行判断，系统不会自动推送。发送文件请使用 send_file_to_user。",
    "parameters": {
      "type": "object",
      "properties": {
        "message": {
          "type": "string",
          "description": "要发送给用户的文本内容。"
        },
        "platform": {
          "type": "string",
          "enum": ["微信", "QQ", "飞书"],
          "description": "目标推送平台。"
        }
      },
      "required": ["message", "platform"],
      "additionalProperties": false
    }
  }
}
